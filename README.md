# Small-Password-Manager-Utility
Just a small password manager/utility thingy I wrote in Python. It can randomly generate passwords and store them in a Json file if needed. This Json file can be read from inside the program.
